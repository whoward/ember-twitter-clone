<div class='row'>
  <div class='col-xs-3'>
    {{profile-view user=model.user}}
  </div>
  <div class='col-xs-6'>
    {{#each model.tweets as |tweet|}}
      {{inline-tweet tweet=tweet}}
    {{/each}}
  </div>
</div>